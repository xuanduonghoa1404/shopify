import { useEffect, useLayoutEffect } from 'react';
import { isServer } from './target.ts.esnext';

const useIsomorphicLayoutEffect = isServer ? useEffect : useLayoutEffect;

export { useIsomorphicLayoutEffect };
